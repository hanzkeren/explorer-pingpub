import { NameService, Network, RpcURLs } from './name-service';
export declare const serviceID = "bdd";
export declare class BDD extends NameService {
    serviceID: string;
    chain: string;
    contractAddress: {
        mainnet: string;
        testnet: string;
    };
    resolve(name: string, network: Network): Promise<string>;
    lookup(address: string, network: Network, options?: {
        rpcUrls?: RpcURLs;
    }): Promise<string>;
}
