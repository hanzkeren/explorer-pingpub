import { AllowedTopLevelDomains, ChainAddressResult, NameResult, NameService, Network, RpcURLs } from './name-service';
export declare const serviceID = "celestialsId";
export declare class CelestialsId extends NameService {
    serviceID: string;
    chain: string;
    contractAddress: {
        mainnet: string;
        testnet: string;
    };
    private getSupportedChains;
    resolve(name: string, network: Network, options?: {
        allowedTopLevelDomains?: AllowedTopLevelDomains;
        rpcUrls?: RpcURLs;
    }): Promise<ChainAddressResult[]>;
    lookup(address: string, network: Network, options?: {
        rpcUrls?: RpcURLs;
    }): Promise<NameResult[]>;
}
