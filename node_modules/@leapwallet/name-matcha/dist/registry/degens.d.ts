import { NameService, Network } from './name-service';
export declare const serviceID = "degeNS";
export declare class DegeNS extends NameService {
    serviceID: string;
    chain: string;
    contractAddress: {
        mainnet: string;
        testnet: string;
    };
    resolve(name: string, network: Network): Promise<string>;
    lookup(address: string, network: Network): Promise<string>;
}
