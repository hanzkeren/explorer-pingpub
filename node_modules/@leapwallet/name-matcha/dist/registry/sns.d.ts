import { NameService, Network } from './name-service';
export declare const serviceID = "sns";
export declare class SNS extends NameService {
    serviceID: string;
    chain: string;
    contractAddress: {
        mainnet: string;
        testnet: string;
    };
    resolve(name: string, network: Network): Promise<string>;
    lookup(address: string, network: Network): Promise<string>;
}
