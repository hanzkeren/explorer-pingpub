import { AllowedTopLevelDomains, NameService, Network } from '../name-service';
type SupportedSpaceIdDomains = 'inj' | 'sei';
export declare const serviceID = "spaceIds";
export declare class SpaceIds extends NameService {
    serviceID: string;
    chain: string[];
    contractAddress: Record<SupportedSpaceIdDomains, Record<Network, string>>;
    resolve(name: string, network: Network, options?: {
        allowedTopLevelDomains?: AllowedTopLevelDomains;
    }): Promise<string>;
    lookup(address: string, network: Network): Promise<string>;
}
export {};
